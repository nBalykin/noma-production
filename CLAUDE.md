# NOMA Production — сайт-портфолио

TL;DR для Claude: одностраничный сайт-портфолио музыкальной продакшн-студии. Читай этот файл вместо того, чтобы заново прояснять контекст.

## Поддержание этого файла в актуальном состоянии

Этот файл должен отражать текущее состояние проекта, а не историю. Если в ходе работы выясняется что-то, что противоречит написанному ниже (изменился факт, дизайн-решение, договорённость с клиентом, сам код стал вести себя иначе) — **обнови соответствующий раздел сразу, в рамках той же правки**, не дожидаясь отдельной просьбы. Не оставляй файл рассинхронизированным с реальным `studio-landing-mockup.html`.

## Что это и где смотреть

- **Рабочий файл:** `studio-landing-mockup.html` — единственный файл, который редактируется. Это самодостаточный HTML/CSS/JS (без сборки, без фреймворков) — открывается напрямую в браузере.
- **Опубликован как Artifact:** https://claude.ai/code/artifact/43b4380e-1745-491f-8cc2-84c249342686
  Чтобы обновить именно эту ссылку (а не создать новую): сначала `Artifact` c `action: "read"` и этим `url`, затем публиковать с тем же `url`.
- **`Main.dc.html` + `canvas.json`** — заброшенная попытка сделать интерактивный Claude Design canvas. На этой машине нет Node.js/Bun, поэтому `seed-canvas.mjs` не запускается и канвас собрать нельзя. Не трогать, не пытаться реанимировать без node/bun. Весь реальный макет живёт в `studio-landing-mockup.html`.
- Фото команды лежат в этой же папке (`Фото Никита.jpg`, `Фото Денис.PNG`) и уже встроены в HTML как base64 (см. ниже, как добавлять новые).
- **`Pictures/`** — оригиналы обложек треков (не сжатые, полноразмерные JPG). В HTML встроены только сжатые копии (base64), оригиналы отсюда не трогаются и не удаляются — используются как источник при пересжатии.
- **`Audio/`** — сырые WAV-мастера треков от клиента (~370 МБ суммарно). **Не хостить и не коммитить как есть** — это исходники для конвертации, не то, что отдаётся посетителям сайта.
- **`Audio/web/`** — веб-версии треков, сгенерированные из WAV через ffmpeg: для каждого трека пара `<slug>.opus` (128k) + `<slug>.mp3` (192k, фолбэк для старого Safari/iOS без поддержки Opus). Именно на эти файлы ссылаются `<audio>` в HTML (`Audio/web/...`). Если клиент добавит новый трек или заменит WAV — пересобрать пару командой вида `ffmpeg -i "input.wav" -c:a libopus -b:a 128k out.opus` и `-c:a libmp3lame -b:a 192k out.mp3`.

## Стек и почему так

- Чистый HTML/CSS/JS, без React/сборщиков — сознательный выбор для лёгкого хостинга (GitHub Pages и т.п.) и чтобы клиент сам мог редактировать текст.
- Один файл, все стили инлайн + один `<style>` блок сверху с CSS-переменными и media query.
- Референс, который клиенту НЕ нравится: milkmusic.ru (светлый, «самодельный» вид, зелёные кнопки) — сайт сделан подчёркнуто иначе: тёмный, строгий, официальный.

## Дизайн-система

- Тёмная палитра, CSS-переменные в `:root`: `--bg:#0d0b12`, `--bg-elevated:#17141f`, текстовые `--text/--text-secondary/--text-tertiary/--text-quiet/--text-faint`.
- **Акцент: сложный синий `--accent:#4C63D2`** (раньше был салатовый `#b8ff3d` — клиент попросил заменить: слишком «кислотно» на чёрном фоне для премиального/строгого позиционирования под топ-артистов и лейблы). Везде используется только через переменные (`--accent`, `--accent-dim`, `--accent-soft`, `--accent-soft-2`, `--accent-bar`, `--on-accent`) — при повторной смене акцентного цвета достаточно поменять блок переменных в `:root`, руками по файлу искать не нужно. `--on-accent` — светлый (`#f5f3f9`), т.к. этот синий средней светлоты и белый текст/иконки на нём читаются лучше тёмных.
- **Тёплые градиенты `--warm-1:#9a5a2e` / `--warm-2:#3c1f16`** — глубокое амбер/медное свечение на фоне hero (радиальный градиент за заголовком), задаёт «премиальную» глубину в паре с холодным синим акцентом. Если добавляешь новые фоновые свечения/блобы в других секциях — используй эту же пару для консистентности, а не произвольный цвет.
- Шрифты: **Unbounded** (заголовки) + **Manrope** (текст) — оба с кириллицей, из Google Fonts.
- **Главный `h1` в hero — `font-weight: 600`** (SemiBold), не 700/800: клиент просил снизить насыщенность шрифта в заголовке, Ultra-Bold/Bold в Unbounded на таком размере выглядел слишком тяжело. Остальные заголовки (`h2`) остаются на 700 — трогать их не просили.
- Стиль сознательно «строгий/официальный»: прямоугольные скругления по 6–12px (НЕ pill-кнопки), никаких ховеров с движением/подпрыгиванием (только смена цвета/яркости), минимум анимации.
- **Логотип в шапке — только текст** («NOMA Production» + подпись «продакшн-студия»), без иконки/значка слева. Раньше рядом был круглый SVG-значок (окружность + вертикальные штрихи) — клиент попросил убрать «бейдж» в левом верхнем углу, он не вписывался в строгий стиль. Не возвращай значок без явного запроса.
- **Кнопка `.btn-ghost` («Обсудить проект» и подобные)** — не просто обводка `var(--border-soft)` (она была почти не видна на тёмном фоне и не читалась как кликабельная). Сейчас: `border: 1px solid rgba(245,243,249,0.4)` + `background: rgba(245,243,249,0.12)` + `color: var(--text)`. При добавлении новых ghost-кнопок бери этот же паттерн (светлая полупрозрачная обводка + лёгкая заливка), а не голый `border-soft`.
- **Иконки в SERVICES STRIP** (Аранжировка / Сведение / Мастеринг) — предметные, не абстрактные: у «Аранжировки» силуэт клавиш синтезатора (в духе Nord Stage — корпус + вертикальные риски-клавиши), у «Сведения» — вертикальные фейдеры микшера (три дорожки с ручками на разной высоте), у «Мастеринга» — два поворотных регулятора (ручки-потенциометры). Раньше были более общие иконки (слои, эквалайзер-линии, вертикальные бары) — если понадобится ещё раз менять эти иконки, держи тот же принцип «похоже на реальное студийное железо», а не абстрактную пиктограмму.
- Мобильная адаптация — один media query `@media (max-width: 860px)` внизу `<style>`: прячет nav-links, схлопывает `.grid-2/.grid-3/.grid-4/.grid-6` в 1 колонку. Если добавляешь новый грид-блок с asymmetric padding/border между колонками (как было в SERVICES STRIP) — обязательно проверь, что он не «съезжает» при схлопывании в 1 колонку на мобильном (была реальная баг-репорт от клиента на это).

## Контент и факты (не выдумывать заново)

- **Название:** NOMA Production.
- **Услуги:** аранжировка под ключ **со сведением** — основная услуга (это единый пакет, не разделять на отдельные позиции в прайсе), плюс отдельно мастеринг. **Звукозапись студия НЕ оказывает** (пишутся в чужой студии) — нигде не упоминать запись/звукозапись как услугу.
- **Команда (в этом порядке везде: Денис → Никита → Максим):**
  - Денис — композитор, аранжировщик, гитарист. Гитарист и муз. руководитель коллектива Филиппа Киркорова; работает с Кириллом Туриченко, группой Те100стерон, Сергеем Куприком (экс-«Лесоповал»). Фото есть.
  - Никита — аранжировщик, пианист. Эстрадный пианист, классическое образование (МГК им. Чайковского), клавишник коллектива Филиппа Киркорова, работает с Кириллом Туриченко. Фото есть.
  - Максим — звукорежиссёр, гитарист. Микс-инженер. **Фото пока нет** — плейсхолдер: тёмный градиент с большой полупрозрачной буквой «М».
- **Контакты:** телефон `+7 985 752-47-11`, Telegram `@denis_musicpr` (https://t.me/denis_musicpr). Email — ещё плейсхолдер `[email@studio.ru]`.
- **Домен:** решение отложено клиентом, не блокер.
- **Треки портфолио (14 шт., реальные названия и исполнители — не выдумывать заново):** LP · Белый снег (Алекс Островский) · Вернуться (Алекс Островский) · Вечно (MAMUT) · Ласточка (MAMUT) · Падаем в небо (MAMUT) · Рассветы и туманы (Алекс Островский) · Сила России — **два разных трека с одинаковым названием**, оба в списке отдельными строками: Алекс Островский и Алина Шилёмина · Те100стерон · уезжаю (Алекс Островский) · Голос, который не сломать (Алина Шилёмина) · Луганск - мой родной (Алина Шилёмина) · Шторм (Ольга Юсипова). Жанр всё ещё плейсхолдер `[Жанр · 2026]` у всех — реальные жанры клиент не присылал.

## Что ещё плейсхолдер (жду от клиента)

- Email.
- Фото Максима.
- Фото для секции «О нас» (студия/команда за работой — не сольный портрет).
- Текст истории студии во врезке в «О нас».
- Обложки для 2 треков: «Сила России» (версия Алины Шилёминой) и «Голос, который не сломать» — пока тёмный градиентный плейсхолдер без фото (как у Максима, только без буквы). Обложки остальных 12 треков — реальные, от клиента.
- Реальные жанры треков (сейчас у всех `[Жанр · 2026]`).
- Реальные цены (сейчас `от [XXXX] ₽`).
- Отзывы (сейчас 3 плейсхолдер-карточки).

## Механики на странице (уже реализовано, рабочее)

- **Портфолио:** список треков (не сетка карточек). Обложка+название+жанр в строке; у части треков справа маленькая вертикальная миниатюра с кружком-плеем (намёк на рилс, БЕЗ подписи текстом — так и должно быть, это осознанное решение). Реелы не у всех треков — это нормально, элемент просто не рендерится для трека без рилса. Первые 5 строк видны, остальные в `#tracks-more` (`hidden`), разворачиваются кнопкой `#tracks-toggle` (ванильный JS в конце файла). Сейчас 14 треков: 5 видно + 9 в спойлере — если меняешь количество строк, не забудь поправить и текст кнопки (`#tracks-toggle-label`), и совпадающее число в JS-обработчике клика по ней.
- **Аудио-превью на треках:** акцентный кружок (цвет — `var(--accent)`, сейчас синий) с play-иконкой поверх обложки (уже был в разметке как декоративный элемент — теперь это реальная кнопка `.track-cover-play` с `data-audio="<slug>"`). Клик проигрывает `<audio data-audio-id="<slug>">` рядом (двойной источник: `Audio/web/<slug>.opus` + `.mp3`-фолбэк), меняет иконку на паузу (`.play-icon-path`, свап атрибута `d` между треугольником и паузой через JS в конце файла), при клике на другой трек текущий останавливается — играет только один трек одновременно. У треков без реальной обложки (см. плейсхолдеры выше) та же кнопка лежит прямо на тёмном градиенте вместо фото. JS-логика — в самом низу `<script>`, ищи `track-cover-play`.
- **Волны на треках** — генерируются JS (`seededRand`/`fillBars`), не хардкожены. Раньше в hero под кнопками была ещё и эквалайзер-полоска (`#eq-strip`, тот же `fillBars`) — клиент попросил убрать, она «разрывала» вёрстку между кнопками и SERVICES STRIP; сейчас там просто пусто, JS-инициализация тоже удалена (не оставляй мёртвый код, если возвращаешь — переделывай заново под новую вёрстку).
- **Карточки команды:** большое фото на всю карточку (`aspect-ratio: 3/4`), внизу — фиксированной высоты (160px) панель с именем/ролью/био на эффекте матового стекла (`backdrop-filter: blur`), текст на фото, а не рядом с ним. Высота панели одинаковая у всех трёх карточек намеренно (было исправление бага с «плавающим» уровнем текста) — **не делай её снова `height: auto`/по контенту**, иначе имена опять разъедутся по вертикали между карточками.
  - Денис и Максим: обычный `background-image` + `background-size: cover` на самой `.team-photo-card`.
  - **Никита — особый случай:** его исходник (`Фото Никита.jpg`) всего 640×640, уже плотно скадрирован клиентом, свободных пикселей по краям нет. Простой `cover` даёт слишком крупное лицо (сильно крупнее, чем у Дениса) и обрезает пианино сбоку — увеличить кадр физически некуда. Решение — два слоя фона внутри карточки вместо одного: нижний слой — то же фото на `background-size: cover` с `filter: blur(26px) brightness(0.55)` и `transform: scale(1.2)` (растяжка, чтобы блюр не показал свой край), верхний — то же фото целиком, без обрезки, на `background-size: auto 58%` по центру. Даёт тот же «крупный план», что у Дениса, без обрезки и с мягкой размытой подложкой вместо пустых полей. Если клиент пришлёт новое, более просторное фото Никиты — можно вернуть обычный `cover`.

## Как добавлять/менять фото

Файлы вставляются как `background-image: url('data:image/jpeg;base64,...')` прямо в HTML (сайт — один самодостаточный файл). Т.к. base64 очень длинный, **не вставляй его вручную через Edit** — используй PowerShell:

```powershell
Add-Type -AssemblyName System.Drawing
# ресайз/сжатие в JPEG ~800-900px по стороне, quality ~80-85, если оригинал большой
# затем [Convert]::ToBase64String(bytes) и .Replace() нужного места в файле через
# [System.IO.File]::ReadAllText/WriteAllText с New-Object System.Text.UTF8Encoding($false)
# (важно: без BOM, иначе кириллица может испортиться)
```

Смотри историю правок в этом же чате/сессии как пример точных команд (ресайз фото Дениса, вставка фото Никиты).

## Хостинг (уже обсуждено с клиентом, решение принято)

Рекомендовано и согласовано: **GitHub Pages** как основной хостинг (бесплатно, статике достаточно), домен покупается отдельно у любого регистратора и направляется DNS-записями. Клиент сказал, что готов немного разобраться с git.

**Аудио — статус изменился.** Изначально была договорённость не хостить аудио/видео самим, а встраивать плееры (Яндекс.Музыка/VK/YouTube). Клиент прислал реальные WAV-мастера треков и попросил подключить прямо на сайте — сейчас превью треков в портфолио играют самостоятельно захостенные Opus/MP3 (см. `Audio/web/` выше), а не внешние плеер-виджеты. Суммарный вес `Audio/web/` — около 55 МБ на 14 треков (сжаты из ~370 МБ WAV), это уже приемлемо для GitHub Pages. Сырые WAV-мастера (`Audio/`) коммитить не нужно — это чистые исходники, не то, что должно попасть в репозиторий/на хостинг. Если клиент решит вернуться к идее внешних плееров вместо самостоятельного хостинга — заменить `<audio>`-блоки на iframe-виджеты, структура портфолио (список треков) от этого не изменится.
