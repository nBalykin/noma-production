# Сырые WAV-мастера от клиента — исходники для конвертации, не для хостинга
Audio/*.wav
Audio/*.WAV

# Оригиналы обложек треков (несжатые) — источник для base64, уже встроены в HTML
Pictures/

# Пустая папка
Video/

# Заброшенная попытка canvas-редактора — не часть сайта
Main.dc.html
canvas.json

# Оригиналы фото команды — уже встроены в HTML как base64
Фото Никита.jpg
Фото Денис.PNG
Фото Макс.PNG

# Оригиналы фото наград — уже встроены в HTML как base64
Награды/

# Локальные настройки сессии
.claude/
